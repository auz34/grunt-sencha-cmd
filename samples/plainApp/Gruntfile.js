'use strict';

module.exports = function(grunt) {

    grunt.loadNpmTasks('grunt-sencha-cmd');

    // Project configuration.
    grunt.initConfig({
        sencha_cmd: {
            app :{}
        }
    });

};

